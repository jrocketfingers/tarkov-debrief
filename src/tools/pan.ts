import * as paper from "paper";
import { SetToolFn, Tool, ToolType } from "./tool";

let startPoint: paper.Point;

export const usePan = (canvasInstance: paper.PaperScope | null, setToolOuter: SetToolFn, toolOuter: Tool) => {
}